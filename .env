# Backend Environment Variables
MLFLOW_TRACKING_URI=http://mlflow:5000
PYTHONUNBUFFERED=1

# Weights & Biases Configuration
# Replace with your actual W&B API key
WANDB_API_KEY=88d94e588a90f4e58d6c1ed82980e3a50cd9fdee
WANDB_PROJECT=flappy-bird-rl
# Optional: Set your W&B entity/username
WANDB_ENTITY=codingpracticework

# GPU Configuration (if available)
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=all
TF_FORCE_GPU_ALLOW_GROWTH=true
# Set to Dockerfile.gpu to use GPU-specific Dockerfile
# USE_GPU=Dockerfile.gpu
# Set to 1 to enable GPU in Docker
GPU_COUNT=1

# MLflow Configuration
MLFLOW_DEFAULT_ARTIFACT_ROOT=/mlflow/artifacts

# Frontend Configuration
NEXT_PUBLIC_API_BASE_URL=/api 